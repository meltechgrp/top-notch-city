query QueryPlaceAutocomplete(
  $input: String!
  $region: String!
  $placeType: PlaceType
) {
  queryPlaceAutocomplete(
    input: $input
    region: $region
    placeType: $placeType
  ) {
    description
    placeId
    mainText
    secondaryText
  }
}
